import { format } from '../../source/formats/exit'

describe('the exit format function', () => {
  it('should render passed in data correctly', () => {
    const expectData = ''
    const actualData = format('')
    expect(actualData).toBe(expectData)
  })
})
